<?xml version="1.0" encoding="utf-8" ?>
<config version="1">

	<!-- Logging settings mode=[Off|Override|Append], debug=[true|false]-->
    <log modus="Off" debug="false" path="/var/log/" encoding="utf-8" filename="webexpress.log" timepattern="dd.MM.yyyy HH:mm:ss" />

	<endpoint uri="http://localhost/"/>

	<kestrel>
		<maxconcurrentconnections>300</maxconcurrentconnections>
		<maxrequestbodysize>3000000000</maxrequestbodysize>
		<maxrequestheaderstotalsize>65536</maxrequestheaderstotalsize>
	</kestrel>

	<!-- Session and its cookie. Optional; omit to keep the defaults (30-day sliding
	     lifetime, Secure tracking the request scheme). timeout is in minutes; a
	     non-positive value disables expiry. Set secure=true behind a TLS-terminating
	     proxy so the cookie is marked https-only even though the server sees http. -->
	<session>
		<timeout>43200</timeout>
	</session>

	<culture>en-US</culture>

	<packages>./packages</packages>
	<assets>./assets</assets>
	<data>./data</data>
	<contextpath></contextpath>
    
</config>